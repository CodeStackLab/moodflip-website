'use client';
import React,{useEffect,useState} from 'react';
import Header from '@/components/Header';

type Provider='openrouter'|'openai'|'anthropic'|'gemini';
type Model={id:string;name:string;context?:number};
const providerLabels:{[K in Provider]:string}={openrouter:'OpenRouter',openai:'OpenAI',anthropic:'Claude / Anthropic',gemini:'Google Gemini'};
export default function AiControlPage(){
 const [settings,setSettings]=useState<any>({enabled:true,primaryProvider:'openrouter',primaryModel:'openai/gpt-4o-mini',fallbackProvider:'gemini',fallbackModel:'gemini-2.0-flash',autoFallback:true,openrouterModels:[]});
 const [availability,setAvailability]=useState<Record<string,boolean>>({}); const [busy,setBusy]=useState(''); const [message,setMessage]=useState('');
 const load=()=>fetch('/api/admin/ai-settings').then(async r=>{if(r.status===401){location.href='/';return null}return r.json()}).then(d=>{if(d){setSettings(d.settings);setAvailability(d.availability)}});
 useEffect(()=>{load()},[]);
 const save=async()=>{setBusy('save');setMessage('');const r=await fetch('/api/admin/ai-settings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(settings)});const d=await r.json();setBusy('');setMessage(r.ok?'AI routing saved.':d.error||'Save failed');};
 const sync=async()=>{setBusy('sync');setMessage('');const r=await fetch('/api/admin/ai-settings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'sync'})});const d=await r.json();setBusy('');if(r.ok){setSettings((s:any)=>({...s,openrouterModels:d.models}));setMessage(`${d.models.length} OpenRouter models synced.`)}else setMessage(d.error||'Sync failed');};
 const models:Model[]=Array.isArray(settings.openrouterModels)?settings.openrouterModels:[];
 return <div className="ai-shell"><Header/><style>{`
 .ai-shell{min-height:100vh;background:radial-gradient(circle at 10% 0,rgba(239,108,168,.13),transparent 30rem),radial-gradient(circle at 90% 10%,rgba(86,200,207,.13),transparent 32rem),#f7f7ff;color:#18152b}.ai-wrap{width:min(1160px,calc(100% - 32px));margin:32px auto 80px}.ai-top{display:flex;justify-content:space-between;align-items:end;gap:20px;margin-bottom:24px}.ai-kicker{color:#6654e8;font-size:12px;font-weight:900;letter-spacing:.12em;text-transform:uppercase}.ai-top h1{font:600 clamp(34px,5vw,58px)/1.02 Newsreader,serif;margin:8px 0}.ai-top p{color:rgba(24,21,43,.62);max-width:650px}.ai-back{padding:11px 16px;border:1px solid rgba(73,61,139,.14);border-radius:999px;background:#fff;text-decoration:none;color:#5543d8;font-weight:800}.ai-grid{display:grid;grid-template-columns:1.2fr .8fr;gap:18px}.ai-card{background:rgba(255,255,255,.86);border:1px solid rgba(73,61,139,.14);border-radius:26px;padding:24px;box-shadow:0 24px 60px rgba(55,43,120,.08)}.ai-card h2{margin:0 0 6px;font:600 25px Newsreader,serif}.ai-sub{color:rgba(24,21,43,.56);font-size:13px;margin-bottom:22px}.ai-field{display:grid;gap:7px;margin:16px 0}.ai-field label{font-size:12px;font-weight:900;text-transform:uppercase;letter-spacing:.07em}.ai-field select,.ai-field input{width:100%;padding:12px 13px;border:1px solid rgba(73,61,139,.16);border-radius:12px;background:#fafaff;color:#18152b}.ai-row{display:grid;grid-template-columns:1fr 1.4fr;gap:12px}.ai-toggle{display:flex;align-items:center;gap:10px;margin:16px 0;font-weight:750}.ai-toggle input{width:18px;height:18px;accent-color:#6654e8}.ai-btns{display:flex;gap:10px;flex-wrap:wrap;margin-top:22px}.ai-btn{border:0;border-radius:999px;padding:12px 18px;background:linear-gradient(135deg,#6654e8,#4b3bc5);color:#fff;font-weight:850;cursor:pointer}.ai-btn.alt{background:#eeebff;color:#5543d8}.ai-btn:disabled{opacity:.6}.provider{display:flex;align-items:center;justify-content:space-between;padding:13px 0;border-bottom:1px solid rgba(73,61,139,.1)}.dot{width:9px;height:9px;border-radius:50%;display:inline-block;margin-right:9px;background:#ef5350}.dot.on{background:#20b47a;box-shadow:0 0 0 5px rgba(32,180,122,.1)}.flow{display:grid;gap:10px;margin-top:18px}.flow div{padding:13px;border-radius:14px;background:#f5f3ff;font-size:13px;font-weight:750}.msg{margin-top:14px;color:#5543d8;font-weight:800;font-size:13px}@media(max-width:780px){.ai-grid{grid-template-columns:1fr}.ai-row{grid-template-columns:1fr}.ai-top{align-items:start;flex-direction:column}}
 `}</style><main className="ai-wrap"><div className="ai-top"><div><div className="ai-kicker">Admin · Intelligence Layer</div><h1>AI Control Center</h1><p>Choose the primary model, define an automatic fallback, and keep MoodFlip available with database-backed actions when providers are unavailable.</p></div><a className="ai-back" href="/admin">← Admin dashboard</a></div><div className="ai-grid"><section className="ai-card"><h2>Routing configuration</h2><p className="ai-sub">Keys stay in server environment variables and are never returned to this browser.</p><label className="ai-toggle"><input type="checkbox" checked={settings.enabled} onChange={e=>setSettings({...settings,enabled:e.target.checked})}/>AI-powered mood flipping enabled</label><div className="ai-row"><div className="ai-field"><label>Primary provider</label><select value={settings.primaryProvider} onChange={e=>setSettings({...settings,primaryProvider:e.target.value})}>{Object.entries(providerLabels).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></div><div className="ai-field"><label>Primary model</label>{settings.primaryProvider==='openrouter'&&models.length?<select value={settings.primaryModel} onChange={e=>setSettings({...settings,primaryModel:e.target.value})}>{models.map(m=><option key={m.id} value={m.id}>{m.name}</option>)}</select>:<input value={settings.primaryModel} onChange={e=>setSettings({...settings,primaryModel:e.target.value})}/>}</div></div><label className="ai-toggle"><input type="checkbox" checked={settings.autoFallback} onChange={e=>setSettings({...settings,autoFallback:e.target.checked})}/>Automatically use fallback when primary fails</label><div className="ai-row"><div className="ai-field"><label>Fallback provider</label><select value={settings.fallbackProvider} onChange={e=>setSettings({...settings,fallbackProvider:e.target.value})}>{Object.entries(providerLabels).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></div><div className="ai-field"><label>Fallback model</label><input value={settings.fallbackModel} onChange={e=>setSettings({...settings,fallbackModel:e.target.value})}/></div></div><div className="ai-btns"><button className="ai-btn" disabled={!!busy} onClick={save}>{busy==='save'?'Saving…':'Save AI routing'}</button><button className="ai-btn alt" disabled={!!busy} onClick={sync}>{busy==='sync'?'Syncing…':`Sync OpenRouter models${models.length?` (${models.length})`:''}`}</button></div>{message&&<div className="msg">{message}</div>}</section><aside className="ai-card"><h2>Provider health</h2><p className="ai-sub">Configured status is detected securely from server environment variables.</p>{Object.entries(providerLabels).map(([v,l])=><div className="provider" key={v}><span><i className={`dot ${availability[v]?'on':''}`}/>{l}</span><strong>{availability[v]?'Ready':'Key required'}</strong></div>)}<div className="flow"><div>1 · Primary AI model</div><div>2 · Automatic fallback model</div><div>3 · Database action prompt</div><div>4 · Built-in MoodFlip library</div></div></aside></div></main></div>
}